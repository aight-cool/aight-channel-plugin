{
  "name": "aight-channel",
  "version": "0.2.0",
  "description": "Aight mobile channel for Claude Code — chat with your session from your phone via cloud relay",
  "type": "module",
  "bin": "./src/index.ts",
  "scripts": {
    "start": "bun install --no-summary && bun src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "qrcode-terminal": "^0.12.0"
  }
}
