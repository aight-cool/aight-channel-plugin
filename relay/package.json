{
  "name": "channel-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Workers WebSocket relay for Aight Channel Plugin — enables remote connections without port forwarding",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250313.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}
